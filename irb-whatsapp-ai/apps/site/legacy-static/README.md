Este diretório nao guarda mais as paginas HTML antigas.

O site oficial da IRB agora vive em `apps/site` como aplicacao Next.js exportada estaticamente.

Se for preciso recuperar o legado por auditoria ou comparacao visual, use:
- o historico do Git
- os backups do servidor em `/opt/irb-whatsapp-ai/backups/`

Nao reintroduza `index.html`, `agendamento.html`, `pagamento.html` ou `confirmado.html` neste app.
